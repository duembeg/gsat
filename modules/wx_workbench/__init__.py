"""Classic wxPython workbench for gsat (maintenance only).

Launch with ``gsat-wx.py``. New feature UI belongs on the PySide workbench.
This package does not import wx at import time so tests can load the path
without a wxPython venv.
"""
